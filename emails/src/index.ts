export type {
  ActionEmailStrings,
  CommonStrings,
  EmailStrings,
} from './i18n/index';
export { DEFAULT_BRAND_NAME, renderEmail } from './render';
export { EMAIL_LOCALES } from './types';
export type {
  EmailLocale,
  EmailTemplate,
  RenderedEmail,
  RenderOptions,
} from './types';
